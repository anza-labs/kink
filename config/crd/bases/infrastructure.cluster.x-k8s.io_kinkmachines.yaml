apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: kinkmachines.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    kind: KinkMachine
    listKind: KinkMachineList
    plural: kinkmachines
    singular: kinkmachine
  scope: Namespaced
  versions:
    - name: v1alpha1
      schema:
        openAPIV3Schema:
          description: KinkMachine is the Schema for the kinkmachines API.
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: KinkMachineSpec defines the desired state of KinkMachine.
              properties:
                providerID:
                  description: providerID must match the provider ID as seen on the node object corresponding to this machine.
                  type: string
              type: object
            status:
              description: KinkMachineStatus defines the observed state of KinkMachine.
              properties:
                ready:
                  description: Ready denotes that the kink machine infrastructure is fully provisioned.
                  type: boolean
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
